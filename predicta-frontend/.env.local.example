# URL base da API Predicta (sem autenticação).
# Dev local (backend Django em runserver):
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
# Produção (Render) — ajustar para a URL final de deploy:
# NEXT_PUBLIC_API_BASE_URL=https://hackathon-ia-2026.onrender.com

# 1 = dashboard usa dados sintéticos gerados no navegador, sem chamar a API
# (útil enquanto o backend não tem tarifas/sinal carregados). A UI avisa que é exemplo.
NEXT_PUBLIC_USE_MOCK=0
